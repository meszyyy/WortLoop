export interface Card {
  /** Tatoeba sentence id of the German sentence. */
  id: number;
  /** German expression (front of the card). */
  de: string;
  /** Hungarian translation (back of the card). */
  hu: string;
}
